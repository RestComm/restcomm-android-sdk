<resources>

    <!-- Base application theme. In order to be able to use -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorTextSecondary</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- TODO: Failed attempt to change the Dialog Style, and specifically header color
        <item name="alertDialogStyle">@style/AppTheme_Dialog_Alert</item>
        -->
    </style>

    <!-- TODO: Failed attempt to change the Dialog Style, and specifically header color
    <style name="AppTheme_Dialog_Alert" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowTitleStyle">@style/DialogWindowTitleStyle.App</item>
    </style>

    <style name="DialogWindowTitleStyle.App" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:textAppearance">@style/TextAppearance.App.DialogWindowTitle</item>
    </style>

    <style name="TextAppearance.App.DialogWindowTitle" parent="Base.TextAppearance.AppCompat.Title">
        <item name="android:textSize">@dimen/abc_text_size_title_material</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>
    -->

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:textColorPrimary">#FFFFFF</item>
    </style>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
